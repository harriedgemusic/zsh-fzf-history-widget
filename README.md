# zsh-fzf-history-widget

Плагин для zsh, обеспечивающий интерактивный поиск команд из истории с помощью fzf в iTerm2 на macOS.

## Описание

`zsh-fzf-history-widget` — это плагин для zsh, который предоставляет удобный интерактивный интерфейс для поиска и выбора команд из истории терминала. Плагин использует встроенную команду `fc` для чтения истории и `fzf` для отображения интерактивного меню с временными метками.

## Возможности

- 🔍 **Интерактивное меню поиска**: Открывается по нажатию Shift+↑
- 🕐 **Временные метки**: Каждая команда отображается с датой и временем выполнения в неярком оранжевом цвете
- ⚡ **Автодополнение**: Поиск автоматически фильтрует команды на основе уже введённого текста
- ⌨️ **Навигация**: Стрелки вверх/вниз для перемещения по списку
- ➡️ **Быстрый выбор**: Стрелка вправо или Enter для принятия команды
- ✨ **Чистый вывод**: В строку ввода попадает только команда без временных меток и разделителей
- 🍎 **Совместимость с macOS**: Использует `perl` вместо `awk` и `tail -r` вместо `tac`

## Системные требования

- **ОС**: macOS
- **Терминал**: iTerm2 (рекомендуется)
- **Shell**: zsh
- **Зависимости**: 
  - `fzf` (устанавливается через: `brew install fzf`)
  - `perl` (предустановлен в macOS)

## Установка

### Шаг 1: Установка fzf

```bash
brew install fzf
```

### Шаг 2: Создание плагина

**Если вы используете Oh My Zsh**:

```bash
# Клонируйте репозиторий
git clone https://github.com/harriedgemusic/zsh-fzf-history-widget.git \
  ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-fzf-history-widget
```

**Если вы не используете Oh My Zsh**:

```bash
# Клонируйте репозиторий
mkdir -p ~/zsh-plugins
git clone https://github.com/harriedgemusic/zsh-fzf-history-widget.git \
  ~/zsh-plugins/zsh-fzf-history-widget
```

### Шаг 3: Активация плагина

Добавьте в ваш `~/.zshrc`:

**Для Oh My Zsh**:
```bash
plugins=(git zsh-fzf-history-widget)
```

**Без Oh My Zsh**:
```bash
source ~/zsh-plugins/zsh-fzf-history-widget/fzf-history-widget.plugin.zsh
```

### Шаг 4: Настройка iTerm2

1. Откройте **iTerm2 → Preferences → Profiles → Keys**
2. Убедитесь, что **Shift+↑** отправляет последовательность `^[[1;2A`
3. Если привязка не настроена:
   - Нажмите **+** (добавить новую привязку)
   - **Keyboard Shortcut**: Нажмите Shift+↑
   - **Action**: Send Escape Sequence
   - **Esc+**: введите `[1;2A`

### Шаг 5: Применение изменений

```bash
source ~/.zshrc
```

## Использование

### Основные комбинации клавиш

| Комбинация | Действие |
|-----------|----------|
| **Shift+↑** | Открыть меню поиска по истории |
| **↑ / ↓** | Навигация по списку команд |
| **→ / Enter** | Принять выбранную команду |
| **Esc** | Закрыть меню без выбора |
| **Печать текста** | Фильтрация команд в реальном времени |

### Примеры использования

**Пример 1: Поиск всех команд**
1. Нажмите **Shift+↑**
2. Появится меню со всей историей команд
3. Используйте стрелки для навигации
4. Нажмите **→** или **Enter** для выбора

**Пример 2: Поиск с фильтрацией**
1. Введите начало команды, например: `git commit`
2. Нажмите **Shift+↑**
3. Меню откроется с уже отфильтрованными результатами
4. Выберите нужную команду

**Пример 3: Поиск docker команд**
1. Нажмите **Shift+↑**
2. Начните печатать: `docker`
3. Список автоматически отфильтруется
4. Выберите нужную команду стрелками и примените

## Формат отображения

Каждая запись в меню отображается в формате:

```
2026-02-16 03:18 │ git commit -m "Update README"
```

- **Дата и время**: Неяркий оранжевый цвет (ANSI 208)
- **Разделитель**: `│` (вертикальная черта)
- **Команда**: Обычный цвет

При выборе в строку ввода попадает **только команда** без временной метки и разделителя.

## Настройка

### Изменить размер окна меню

Откройте файл плагина и найдите строку с `--height=40%`:

```bash
--height=60%  # Увеличить до 60%
```

### Изменить цвет временных меток

Измените ANSI-код цвета в строке с `\x1b[38;5;208m`:

```bash
\x1b[38;5;214m  # Более яркий оранжевый
\x1b[38;5;166m  # Более тёмный оранжевый
\x1b[38;5;245m  # Серый
```

## Совместимость с другими плагинами

### zsh-autosuggestions

Плагин полностью совместим с `zsh-autosuggestions`. Стрелка вправо будет принимать автоподсказки, когда курсор находится в конце строки.

### zsh-syntax-highlighting

Полная совместимость. Плагин не конфликтует с подсветкой синтаксиса.

## Устранение неполадок

### Проблема: Shift+↑ не открывает меню

**Решение**: Проверьте привязку клавиш в iTerm2:
```bash
# Проверьте текущую привязку
bindkey | grep "1;2A"

# Если пусто, добавьте вручную в ~/.zshrc
bindkey '^[[1;2A' fzf-history-widget
```

### Проблема: Команда не найдена: fzf

**Решение**: Установите fzf:
```bash
brew install fzf
```

### Проблема: История пустая

**Решение**: Убедитесь, что `EXTENDED_HISTORY` включена:
```bash
setopt EXTENDED_HISTORY
```

### Проблема: Временные метки не отображаются

**Решение**: Проверьте формат истории:
```bash
fc -li 1 | head -5
```

Если метки не отображаются, убедитесь, что в `.zshrc` установлено:
```bash
setopt EXTENDED_HISTORY
```

## Лицензия

MIT License

## Автор

Плагин разработан для использования с iTerm2 на macOS с учётом особенностей BSD-версий утилит.

---

**Версия**: 1.0  
**Совместимость**: macOS, iTerm2, zsh 5.0+
